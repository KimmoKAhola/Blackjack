namespace Blackjack
{
    /// <summary>
    /// Enum containing the player's available actions.
    /// HIT, STAND, SPLIT
    /// </summary>
    public enum PlayerAction
    {
        HIT = 1,
        STAND,
        SPLIT,
    }
}
